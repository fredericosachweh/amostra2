from createpercentagefraction import Command as BaseCommand


class Command(BaseCommand):
    help = "Create percentage to fraction names exercises."
    category = 'fracao-de-porcentagem-extenso'
    description = u'{0}/100 (matriz ext.)'
